# Jaime Bohorquez
# Programmed using Atom + Terminal on Ubuntu
# This was derived from the GoLang course at codecademy.com

# GoLang's cmd arguments.

make:
	@go build main.go
clean:
	echo "Nothing was deleted."
	# There is no file extension in a binary for unix based
	# so this is interesting.
all:
	@make clean
	@make
run:
	# runs without compiling.
	go run main.go

# You can run as if it were a script, or compile and run as a binary.
